// Shared by Availability (which positions an employee can work) and Scheduling (which
// position a shift is for).
export enum Position {
  FRONTDESK = 'frontdesk',
  HELPDESK = 'helpdesk',
  INFORMATION = 'information',
  CONSULTATION = 'consultation',
}
